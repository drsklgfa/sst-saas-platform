import test from 'node:test';import assert from 'node:assert/strict';import {encryptBackup,decryptBackup,isEncryptedBackup} from '../src/domain/backup/crypto.ts';test('criptografa e restaura backup',()=>{const raw=Buffer.from('dados importantes');const enc=encryptBackup(raw,'senha-segura');assert.equal(isEncryptedBackup(enc),true);assert.deepEqual(decryptBackup(enc,'senha-segura'),raw);assert.throws(()=>decryptBackup(enc,'errada'))});
