import test from 'node:test';import assert from 'node:assert/strict';import {aggregatePsychosocial} from '../src/domain/engines/psychosocial.ts';test('agrega dimensões e aplica reversão',()=>{const r=aggregatePsychosocial([{dimension:'SUPORTE',score:5,min:1,max:5,reverse:true},{dimension:'SUPORTE',score:4,min:1,max:5,reverse:true}]);assert.equal(r[0].count,2);assert.ok(r[0].score<30)});
